---
url: https://rpgbot.net/dnd5/characters/multiclassing/sorcadin-Guide/
archived_at: 2026-04-01T03:01:19.072Z
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Conversion Failed - into.md</title>
    <meta name="description" content="Error converting webpage to markdown" />
    <link rel="icon" href="/favicon.ico" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      :root {
        --bg: #ffffff;
        --fg: #000000;
        --fg-secondary: #666;
        --shadow: #ddd;
        --error: #ff4444;
        --font: 'SF Mono', "Geist Mono", Consolas, 'Roboto Mono', Menlo, Monaco, 
             'Liberation Mono', 'DejaVu Sans Mono', 'Courier New', 
             Courier, monospace;
      }

      body {
        font-family: var(--font);
        background: var(--bg);
        color: var(--fg);
        min-height: 100vh;
        min-height: 100dvh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
        line-height: 0.8;
        font-size: 14px;
      }

      h1, h2, h3, h4, h5, h6 {
        font-size: inherit;
        font-weight: 600;
      }

      p {
        color: var(--fg);
        line-height: 1.4;
        margin: 8px 0;
      }

      @media (prefers-color-scheme: dark) {
        :root {
          --bg: #000000;
          --fg: #ffffff;
          --fg-secondary: #999;
          --shadow: #333;
          --error: #ff6b6b;
        }
      }

      .container {
        max-width: 500px;
        width: 100%;
        margin-bottom: 40px;
      }

      .error-box {
        border: 1px solid var(--error);
        padding: 12px 20px;
        margin: 20px 0;
        box-shadow: 3px 3px 0px 0px var(--shadow);
        background: var(--bg);
      }

      .error-title {
        color: var(--error);
        margin-bottom: 10px;
      }

      .actions {
        margin-top: 20px;
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
      }

      a {
        color: var(--fg);
        text-decoration: none;
      }

      .actions a {
        padding: 8px 16px;
        border: 1px solid var(--fg);
        background: var(--bg);
        font-family: var(--font);
        font-size: 14px;
        display: inline-block;
      }

      .actions a:hover {
        background: var(--fg);
        color: var(--bg);
      }

      .hotpink {
        color: #FF0099;
      }

      .link-underline {
        background-image: linear-gradient(currentColor, currentColor);
        background-position: 0 100%;
        background-repeat: no-repeat;
        background-size: 100% 1px;
      }
    </style>
  </head>
  <body>
    <div class="container">
     <h2 class="error-title">## Conversion Failed</h2>
      <div class="error-box">
        <p>The webpage was not found (404) or access was denied. Please verify the URL.</p>
        <p>URL: <span style="color: var(--fg-secondary);">https://rpgbot.net/dnd5/characters/multiclassing/sorcadin-Guide</span></p>
      </div>

      <div class="actions">
        <a href="/">← Back to Home</a>
        <a href="javascript:history.back()">← Go Back</a>
      </div>
    </div>
  </body>
</html>