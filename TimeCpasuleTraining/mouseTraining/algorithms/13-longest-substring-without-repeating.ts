// Task: Find the length of the longest substring without repeated characters.

export function lengthOfLongestSubstring(value: string): number {
  throw new Error("TODO");
}

// Measure contiguous substrings by non-repeating UTF-16 code units.
