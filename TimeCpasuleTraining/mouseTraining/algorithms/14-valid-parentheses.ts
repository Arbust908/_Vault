// Task: Check whether every bracket is correctly matched and nested.

export function isValidParentheses(value: string): boolean {
  throw new Error("TODO");
}

// Assume only (), [], and {}; the empty string is valid.
