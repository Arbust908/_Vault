// Task: Use binary search to find a target in a sorted array.

export function binarySearch(
  values: readonly number[],
  target: number,
): number {
  throw new Error("TODO");
}

// Return any matching index, or -1 when the target is absent.
