// Task: Count the separate islands of connected land in a grid.

export type GridCell = 0 | 1;

export function countIslands(
  grid: readonly (readonly GridCell[])[],
): number {
  throw new Error("TODO");
}

// Count four-directionally connected land without mutating the grid.
