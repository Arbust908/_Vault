// Task: Find the maximum area of a square containing only good land.

export type LandCell = 0 | 1;

export function maximalSquareArea(
  land: readonly (readonly LandCell[])[],
): number {
  throw new Error("TODO");
}

// Return the square's area, not its side length, without mutating the input.
