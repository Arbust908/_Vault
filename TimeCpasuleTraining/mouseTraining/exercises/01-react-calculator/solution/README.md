# Solution Workspace

Create the corrected implementation here. Keep the original `broken/` directory unchanged so the exercise remains repeatable.

Suggested files:

- `src/App.tsx`
- `src/calculator.ts` for pure state transitions and arithmetic
- `src/App.test.tsx` for behavior tests
- `notes.md` for diagnosis, assumptions, and tradeoffs
