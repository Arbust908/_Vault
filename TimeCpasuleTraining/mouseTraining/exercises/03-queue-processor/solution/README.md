# Solution Workspace

Implement the processor here. Keep the command model explicit and add tests for ordering, validation, duplicate commands, missing records, and partial failure behavior.

Suggested files:

- `queue.ts`
- `queue.test.ts`
- `notes.md`
