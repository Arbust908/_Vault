# UI Assets

This folder is intended to hold source files or high-resolution versions of UI elements (buttons, icons, frames, etc.) used in the game. The actual game assets should be placed in the `public/img/` directory (or subdirectories) within the Narrat project folder (`proxima-connection-vn`). UI configuration is handled in `narrat/buttons.yaml` and `narrat/screens.yaml`.
